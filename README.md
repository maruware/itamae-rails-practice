# Itamae Rails Practice
This is a Itamae practice for Ruby on Rails environments.  
This sample is assumed a environment with
* CentOS
* nginx
* MySQL

##Usage
```
bundle exec itamae ssh -h <host> -p <port> -u vagrant -i <key> entry.rb -j nodes/app1.json
```